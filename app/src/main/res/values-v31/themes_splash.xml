<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema real de Splash para API 31+ usando Theme.SplashScreen -->
    <style name="Theme.NotasInteligentesValdo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.NotasInteligentesValdo</item>
    </style>
</resources>
